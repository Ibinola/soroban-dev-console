import { Module } from "@nestjs/common";

// RPC module is a placeholder for future proxy endpoints.
// Logging/correlation middleware is applied globally in AppModule.
@Module({})
export class RpcModule {}
