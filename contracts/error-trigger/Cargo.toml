[package]
name = "error-trigger"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "28.0.0", features = ["alloc"] }

[dev-dependencies]
soroban-sdk = { version = "28.0.0", features = ["testutils"] }
